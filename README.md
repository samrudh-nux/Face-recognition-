AI-Powered Classroom Attendance & Analytics System

Built using Python, OpenCV, and face recognition to automate classroom attendance and generate engagement analytics from classroom video streams.

GitHub: <your-link>

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
